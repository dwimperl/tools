

Building Dwimperl

*) Download and install Strawberry perl strawberry-perl-5.12.3.0.msi
*) Open command prompt
*) Check in the whole Strawberry directory to a fresh Git repository
   (Git as that will make it easier to exclude all the files from .git)
